% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general-api.R
\name{bdcn_fit_general}
\alias{bdcn_fit_general}
\title{Fit all-pairs BDCN with an arbitrary known covariate matrix}
\usage{
bdcn_fit_general(
  Y,
  graph,
  covariates,
  train_rows = seq_len(nrow(Y)),
  pre_history = NULL,
  pairs = NULL,
  means = NULL,
  signal = 0,
  label = "BDCN continuous, all ordered pairs",
  control = bdcn_control()
)
}
\arguments{
\item{Y}{Time-by-series nonnegative integer count matrix.}

\item{graph}{A BDCN graph containing \code{edges}, \code{A}, and \code{W}.}

\item{covariates}{Known time-by-covariate design matrix for every row of \code{Y}.}

\item{train_rows}{Consecutive rows used for posterior fitting.}

\item{pre_history}{Optional count history preceding \code{Y}.}

\item{pairs}{Optional all-pairs dictionary from \code{\link[=bdcn_all_pairs]{bdcn_all_pairs()}}.}

\item{means}{Optional positive prior-centering means, one per series.}

\item{signal}{Numeric label used only to derive deterministic random seeds.}

\item{label}{Human-readable fit label.}

\item{control}{A \code{\link[=bdcn_control]{bdcn_control()}} object.}
}
\value{
A \code{bdcn_general_fit} object with posterior draws and frozen inputs.
}
\description{
This is the general BDCN engine used by the Divvy bike example. Each target
has its own coefficients for the columns of \code{covariates}. All ordered lagged
source-target pairs enter the graph-informed shrinkage model; there are no
separate unpenalized self or physical-network lag terms.
}
