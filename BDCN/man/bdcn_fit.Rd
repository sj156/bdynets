% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bdcn_fit}
\alias{bdcn_fit}
\title{Fit a graph-shrinkage BDCN model}
\usage{
bdcn_fit(
  Y,
  graph,
  train_rows = seq_len(nrow(Y)),
  pre_history = NULL,
  pairs = NULL,
  time_covariate = NULL,
  season_covariate = NULL,
  means = NULL,
  W = NULL,
  lag_reference_scale = NULL,
  signal = 0,
  label = "BDCN full dynamic nonlocal domain",
  control = bdcn_control()
)
}
\arguments{
\item{Y}{Time-by-series nonnegative integer count matrix.}

\item{graph}{A graph returned by \code{\link[=bdcn_grid]{bdcn_grid()}} or an equivalent list.}

\item{train_rows}{Rows of \code{Y} used to fit the posterior.}

\item{pre_history}{Optional matrix with at least one count row preceding \code{Y}.}

\item{pairs}{Optional candidate dictionary; defaults to \code{\link[=bdcn_pairs]{bdcn_pairs()}}.}

\item{time_covariate}{Optional known time covariate for all rows of \code{Y}.}

\item{season_covariate}{Optional known seasonal covariate for all rows of \code{Y}.}

\item{means}{Optional prior centering means, one per series.}

\item{W}{Optional physical network matrix used by the static network term.}

\item{lag_reference_scale}{Optional source-specific scale used by a known
data-generating transformation. This is mainly for exact simulation
reproduction; ordinary analyses should leave it \code{NULL}.}

\item{signal}{Numeric scenario label used only for deterministic seeds.}

\item{label}{Human-readable fit label.}

\item{control}{A \code{\link[=bdcn_control]{bdcn_control()}} object.}
}
\value{
A \code{bdcn_fit} object containing posterior draws and frozen inputs.
}
\description{
Counts are transformed with \code{log1p()} lag features centered and scaled from
the training rows only. Deterministic time and seasonal covariates are also
centered on the training period. The fitted object retains this preprocessing
so DSS and sequential prediction use the same frozen transformation.
}
