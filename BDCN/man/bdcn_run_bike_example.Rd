% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general-api.R
\name{bdcn_run_bike_example}
\alias{bdcn_run_bike_example}
\title{Run the packaged Divvy bike example}
\usage{
bdcn_run_bike_example(
  profile = c("smoke", "analysis", "long"),
  data_dir = NULL,
  output_dir = NULL,
  control = NULL,
  reuse = TRUE
)
}
\arguments{
\item{profile}{Bike profile passed to \code{\link[=bdcn_bike_control]{bdcn_bike_control()}}.}

\item{data_dir}{Optional external prepared-data directory; defaults to the
bundled example data.}

\item{output_dir}{Optional result directory. Nothing is written when \code{NULL}.}

\item{control}{Optional explicit bike control object.}

\item{reuse}{Reuse a matching package-generated fit checkpoint when present.}
}
\value{
A \code{bdcn_bike_result} with data, fit, diagnostics, DSS, predictions,
observed-data scores, selection table, and prediction table.
}
\description{
This function fits the general all-pairs BDCN, applies validation-selected
joint DSS, and produces rolling one-step June predictions. Set \code{output_dir}
to save reusable fit and result files. The smoke profile validates execution
only; use the analysis or long profile for substantive runs and inspect
convergence before interpretation.
}
\examples{
bike <- bdcn_bike_data()
dim(bike$Y)
bike$split_table
if (interactive()) {
  result <- bdcn_run_bike_example("smoke")
  result$scores
}
}
