% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmde-mcmc-control.R
\name{gmde_mcmc}
\alias{gmde_mcmc}
\title{Configure one reference MCMC chain}
\usage{
gmde_mcmc(
  iterations = 100L,
  warmup = 50L,
  thin = 1L,
  seed = 90601L,
  ess_max_steps = Inf
)
}
\arguments{
\item{iterations}{Total number of sweeps, including warmup.}

\item{warmup}{Number of initial sweeps to discard.}

\item{thin}{Retain every thin-th post-warmup sweep.}

\item{seed}{Integer seed, or NULL to use and advance the caller's RNG.
With an explicit seed the caller's RNG state is restored on exit.}

\item{ess_max_steps}{Explicit ESS diagnostic cap; default Inf (no cap).
Exceeding a finite cap raises an error, never returns the old state as a draw.}
}
\value{
A gmde_mcmc specification.
}
\description{
Defaults are smoke-test lengths, not an inferential recommendation. Proposal
tuning stays fixed throughout warmup and retained sampling. Thin is applied
after warmup: retain warmup + thin, warmup + 2 * thin, and so on.
}
