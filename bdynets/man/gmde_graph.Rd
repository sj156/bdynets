% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmde-graph.R
\name{gmde_graph}
\alias{gmde_graph}
\title{Construct a graph covariance on series IDs}
\usage{
gmde_graph(
  W_graph,
  unit_ids,
  nu = 1,
  kappa = sqrt(2),
  tau = 1,
  rank = "full",
  eigen_tolerance = 1e-10,
  provenance = list()
)
}
\arguments{
\item{W_graph}{Numeric n-by-n adjacency matrix.}

\item{unit_ids}{Unique character IDs, in adjacency order. If adjacency has
dimension names, both row and column names must match these IDs exactly.}

\item{nu, kappa, tau}{Positive fixed kernel parameters.}

\item{rank}{"full" (default), or an integer from 1 to n.}

\item{eigen_tolerance}{Relative tolerance for zero and tied eigenvalues.}

\item{provenance}{List describing externally supplied graph construction.}
}
\value{
A gmde_graph object including adjacency, Laplacian, Phi, covariance,
IDs, eigenvalues, effective rank and provenance.
}
\description{
Adjacency must be exactly symmetric, nonnegative and zero diagonal. It is
never silently symmetrized. Kernel trace is normalized to n * tau^2.
Eigenvalues are sorted in increasing order; rank requests expand to preserve
the entire numerical zero eigenspace and tied blocks. Truncation changes the
prior. Tiny negative Laplacian eigenvalues within the declared tolerance are
treated as numerical zeros, and their original values are retained.
}
