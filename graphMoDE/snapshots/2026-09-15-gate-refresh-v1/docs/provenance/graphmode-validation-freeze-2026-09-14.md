# D-048 phase B source-freeze provenance

2026-09-14. User authorization: “好的，那就开始B吧”, following the explicit B
implementation/freeze/registration and four6000/1500 validation proposal.
Scope: one B with total25200s science+diagnosis cap; then stop. No formal run,
retry, extension, paper/package revision, remote upload or old-result rewrite.

Freeze only these new implementation files and this provenance plus its manifest:

- `R/graphmode_validation.R`
- `scripts/graphmode-validation.R`
- `scripts/tests/graphmode-validation-deterministic.R`
- `docs/GRAPHMODE_VALIDATION_IMPLEMENTATION_2026-09-14.md`

Seventeen fixed-input/mocked-process groups pass. Coverage includes6000-step
disabled-adaptation replay, full independently seeded initialization, new-generator
integration, four short fixed-tape workers and37-scalar/six-PSM diagnosis, exact
scale/envelope binding, unfrozen refusal, new child/timeout routing, once-only
diagnostic acceptance, pending-file quota failure and tamper/retry rejection.
No actual PG/random response/chain is generated by these tests; RNG state/kind
unchanged. The unmodified A acceptance protocol and older mathematical evidence
are reused rather than presented as new independent audits.

A source is historical b698fd83ef70e60e3b922229f88ee26464149655; all eight entries
in its active freeze manifest match. The loaded identity check covers all old
layers plus B. A's accepted registration/report/final receipt are SHA-256-pinned;
the candidate is read from the original RDS double, not rounded display text.
Read-only candidate/seed/storage precheck reviewed37 prior registration records
and six additional A-related records, with zero collisions for the24 B seeds.
Available space at precheck245.8684GiB, above40GiB startup minimum.

Initial test-harness errors (environment assignment and a test-loop variable
shadowing its RNG-kind snapshot) were corrected before the final17-group pass.
The immutable generation helper's exact-spec guard required leaving its original
panel specification intact and supplying the candidate only to the subsequent
fit-config construction; that integration is covered by a fixed-data regression.
No scientific trial run was used to discover or repair these issues.

Preserve the mixed historical worktree and empty index outside this scoped
commit. The unrelated tracked diff fingerprint, excluding evolving operational
notes/registry, is d89a7374352d50c26784c06a96d8d5d19565f129f4da343c21bbd769cf326bfe.
Actual commit/source/runtime and destination signatures are recorded in the new
external B registration after freeze, before any scientific draw. No Git push.
