# D-051：门控更新次数对照——接入完成后的待确认方案

2026-09-15。用户同意先做新执行器接入与预算方案。**本轮不运行、不冻结提交、
不登记科学实验。以下数值是提交用户确认的方案，不是已授权运行。**
不改变已审计的D-050模块、旧55个B冻结文件、论文、包接口或历史结果。

## 问题与边界

D-050固定次数ESS/MH条件组合通过聚焦独立审计，并不表示guidance混合改善。
本轮检验增加廉价门控更新能否改善当前计算效率，不比较论文的五个内部模型，
不添加第二种路网、外部方法、预测任务或正式重复实验。B原valid=FALSE及
guidance_7的Rhat失败、proportion_10的tail ESS未定义均保留。

## 待确认的数值安排

| 项目 | 方案 |
|---|---|
| 模型/路网 | 仅graphMoDE-W；intertwined-spiral，121节点、168时点 |
| 数据 | 一份全新合成面板；road-voronoi真分区5类，拟合K=10；信号幅度0.3，创新方差0.002 |
| 两组 | m=1对照与m=4候选；每个外层迭代先更新专家一次，再固定Z重复m次ESS+guidance MH，最后分配一次 |
| 初值 | 四份完整分散初值：占用类数1/3/7/10，并分散x/v/专家路径；两组对应链共用完全相同初值 |
| 提议尺度 | 仅从原A的已验收RDS读取完整精度2.26889626877594…；全程固定，不再预热调参 |
| rho | 固定4，仍为临时工程值，不声称已完成共同rho校准 |
| 链长 | 每组四链，每链1200外层步；前600步固定尺度预热，保留601–1200共600份，thin=1 |
| 总量 | 8条新链，共9600外层步、4800保留状态；内部子步不是额外样本 |
| 顺序 | 起点1：m1→m4；起点2：m4→m1；起点3：m1→m4；起点4：m4→m1 |
| 执行资源 | 串行单worker，数值库单线程；同机、相同缓存与观察开关 |

共用新数据/起点使两组具有相同后验目标；各链使用独立的采样种子。
**不是共用随机数实验**：m不同会改变随机接口调用数量，不能声称逐步配对轨迹。
两组也不是两份独立数据重复，不能据此推断普遍优越性。两组都走新版执行器，
避免把新观察开销与历史B运行的计时差混在一起。m=1的数学行为沿用D-050审计证据。

拟定种子（冻结前仍需与既有登记做定向碰撞核对）：

- 数据创新、profile重排、unit重排、response：2026091701–1704，依次对应。
- 四起点Z/v/x/theta：分别2026091711–1714、1721–1724、1731–1734、1741–1744。
- m=1四链：2026091751–1754；m=4四链：2026091761–1764。
- 旧A/B只提供动机、尺度和预算依据，不读取旧响应、初值或检查点来续跑。

旧panel模板保留历史的rho2、0.25及400/200字段，只供不可变数据构造函数检查。
新spec与实际fit/job明确覆盖为rho4、原A尺度、1200/600；不能从嵌套模板误读运行配置。

## 预算与停止规则

旧B实测16949.071秒/24000外层步；线性折算9600步约6780秒（113分钟）。
旧B门控/指导占已测步耗时约1.05%，m=4的额外条件更新可能较便宜；
但新数据、占用、ESS循环、I/O及新增验证开销会改变实际成本。
**预计2–2.5小时只是粗估，不是已测加速或保证。**

- 科学批次子进程硬上限11700秒（3小时15分钟），包含生成、8链与批次验收。
- 单链sweep边界预算1260秒；外部子进程上限1320秒；每条链启动前保留1380秒。
- 一次诊断子进程上限900秒（15分钟）。两阶段预算合计12600秒（3.5小时）。
- 父进程退出取证/最终验收另有短暂开销；这不是严格到秒的整段人机交互墙钟承诺。
- 每300外层步留存只读检查点；不支持恢复、重试、加长或只挑成功链。
- 新目录总量上限12GiB；启动至少24GiB空闲，运行时至少5GiB空闲。
- 任一失败、超时、缺结果/验收、身份变更即停止，保留已完成与失败材料；
  不继续凑满八链，不从已有结果绕过非零退出，不自动进入下一轮。

拟用新外部目录：
`/Users/liuzw/countDLM-local-results/graphmode-gate-refresh-20260915-c1`。
本轮不创建它；确认时必须核对不存在、父目录可用、容量及种子无冲突。
若已存在则重新登记新的编号，不能覆盖或复用。

## 实现与验收

新实现位于 `R/graphmode_refresh_run.R`，独立入口
`scripts/graphmode-refresh-run.R`。旧A/B入口不加载它。

- 复用原数据构造、完整分散初始化、真实PG/FFBS/MH、D-043缓存/被拒建议观察，
  以及D-050完整外层接口；不拷贝重写数学核。
- 新registration/job/checkpoint/result/receipt绑定完整源码、commit、运行环境、
  固定m、尺度、数据/初值摘要和新输出位置。旧registration/checkpoint拒绝。
- 未提交或改变的源码、加载定义不符、原55文件/D-050模块改变时拒绝运行。
- 新观察记录保存各内循环实际接受/提议数、内步位移和外层净位移，以及
  全部专家（包含空专家）耗时、数值审计、被拒建议与最终分配条件。
- 每次外层只保留一次；guidance分母为m×自由坐标数，不能只算最后一轮、
  任意一次接受，或把往返位移抵消后当成没有更新。
- 父进程记录子进程退出与耗时，成功结果必须有零退出和完整匹配证据；
  先验证pending receipt再发布最终receipt，缺失、失败、超时、移动均不得接纳。
- 两组分别调用原37量/四链诊断和六对PSM诊断，不把两组合成八链判断。
  rank/folded Rhat≤1.01、bulk/tail ESS≥400、PSM RMS≤0.05均不改变。
- 比较表保留所有原始失败、常数无信息和NA；增加每组四链完整worker耗时之和
  作分母的ESS/秒（包含预热和worker I/O，不仅非空专家时间）。同时报告批次总耗时。
- guidance_7是第七个排序guidance，不是固定专家7。检查全部连续量、离散支持、
  分区变动与长拒绝段；不能只挑一个改善量或接受率更高就判断成功。

短链Rhat未达标时，ESS/秒仅是原始描述，不是可靠精度认证或有效速度提升。
没有自动选择m的获胜规则；本轮结果只供下一步人工判断，不能自动批准长验证。
即使必要门槛通过，也不证明探索所有模式或解除正式模拟禁令。
本接口不新增离散替代精度规则，也不修改原B的历史结论。

## 运行前仍须完成

本轮23组固定输入/模拟进程检查通过：含实际D-050接口的m1/m4固定随机接口
对照、完整配对输入、八个模拟worker的调度和两组原37量诊断、超时结果拒收、
pending发布失败、路径与源码保护、分母/保留样本/报告完整性和一次性授权。
模拟worker使用8步预设状态，不是运行上述1200步科学实验；真实随机接口被
固定返回替代，RNG种类/状态不变。原55文件及已审计D-050主模块保持一致。

1. 用户确认本方案、预算和实际启动范围；必要时对新增执行接入做聚焦复核。
2. 定向核对种子、空间和新路径，记录适用论文/审计指纹；冻结本轮scoped源码。
3. 在全新外部目录登记真正commit与完整文件/运行环境身份，不把草案当登记。
4. 再取得该登记的明确启动授权。科学运行、一次诊断是分开的入口；若用户授权
   两者，可在科学阶段验收成功后执行一次诊断，再停止。

当前安全检查入口：`Rscript --vanilla scripts/graphmode-refresh-run.R check`。
默认 `status` 不启动任何实验。`prepare` 要求 `--approved-plan`；实际
`run` 和 `diagnose` 还各自要求 `--authorized`，且拒绝未冻结源码。
没有自动调参、自动运行、包安装、共享文件同步或正式实验。

本轮没有修改论文。若采用多次内循环，应在计算步骤/实现说明中明确固定m、
时点顺序、外层保留和完整计时口径；当前只是明确登记的小规模开发试验安排，
不把它暗中视为论文已有单扫描算法的正式新基线。
