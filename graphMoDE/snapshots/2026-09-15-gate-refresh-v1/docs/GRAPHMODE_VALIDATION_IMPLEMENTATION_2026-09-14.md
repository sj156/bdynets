# D-048: independent phase B implementation and run contract

The user explicitly authorized starting B after review of A. This supersedes
the earlier B-not-authorized status only for this bounded validation. No formal
simulation, package/interface/manuscript edit, upload, retry or extension.

## Design and evidence

Use the frozen next-warmup plan's B design: a new independent synthetic panel,
four complete dispersed Z/v/x/theta starts, graphMoDE-W, n121/T168/Kfit10,
provisional rho4 and unchanged priors/thresholds. The only numeric tuning value
transferred from A is its full-precision geometric-mean candidate. Read it from
the accepted A report, pinned by registration/report/receipt SHA-256; never use
rounded text, A's states, A's data or a selected best chain. A remains historical
b698fd8 even after B's scoped commit; require its 47 source files and accepted
evidence unchanged, not equality of current HEAD to historical HEAD.

Four chains each run6000 steps: fixed-scale warmup1–1500, retain1501–6000,
thin1, checkpoint every500. Policy NULL means zero adaptation, frozen from step1.
All four use the same exact scale. Data seeds2026091601–1604; Z/v/x/theta/chain
respectively2026091611–1614/1621–1624/1631–1634/1641–1644/1651–1654.
These are new independent streams; initial-state construction is reused unchanged.

## Additive implementation

`R/graphmode_validation.R` privately routes immutable A/development execution
functions to B's identity, record, spec, entry and diagnosis. Original numerical,
FFBS/PG/MH/gate, complete-initialization and passive-observation functions are
unchanged. B guards additionally require exact candidate scale and NULL adaptation
in every envelope; original stepwise controller replay verifies every transition.
No historical command or registration represents B. Error text from reused low-level
A guards can retain its historical wording; schemas, records, entry and reports
are B-specific. Frozen-source loaded-definition checks still inspect original
loaded functions rather than the private adapters.

Scientific batch hard cap24000s; one diagnostic1200s; total25200s (7h).
Worker5640s, dispatcher5760s, reserve5820s before another chain. Serial worker,
single numerical threads, C locale, installed R/backend only. Stage20GiB,
startup free40GiB, active free5GiB. Checks before/after writes and checkpoint
boundaries retain all failed files; no old directory reuse, removal or recovery.
The diagnostic reuses D-046's pending-to-final receipt protocol: post-write quota
check, bound report/request/zero exit and no failure markers, final atomic rename
as the last I/O. No receipt is inferred from a report file or zero child exit alone.

## Diagnosis and stopping

Retain all original37 scalar outcomes and six PSM comparisons: nonconstant
required quantities require rank/folded Rhat<=1.01 and bulk/tail ESS>=400;
PSM RMS<=.05. Constant discrete quantities retain uninformative/failure handling.
Report guidance coordinates/occupancy decomposition, expert rejection spells,
conditional allocation changes and complete module costs, including empty experts.
These necessary checks cannot prove global convergence or select common rho.
All failures remain visible; no truth ARI, methods ranking or performance claim.

Freeze scoped new source/CLI/test/guide and provenance, verify original A files,
register actual commit/runtime/source, seeds and a new external destination before
any scientific draw. B completed or failed: diagnose available evidence once and
STOP. Do not automatically generate another dataset, retune, extend or publish.

Focused tests and actual registration/outcome are recorded separately, not claimed
by this prospective implementation contract.
