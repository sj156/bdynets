# D-044: registered D-043 execution test

User requested connecting the independently audited development layer, local
source freeze/preregistration, Terminal execution and diagnosis on 2026-09-14.
This supersedes D-043's preparation-only boundary, not the formal-run lock.

## Fixed scope, before any random input

- One fresh synthetic intertwined-spiral graphMoDE-W panel: n121/T168,
  five generating profiles, Kfit10, road-Voronoi allocation, amplitude .3 and
  innovation variance .002. Reuse only D-036's deterministic panel constructor
  and provisional prior settings. No old response/chain/checkpoint reuse.
- Rho is fixed at integer 4 for all four starts. This is a provisional engineering
  condition motivated by D-041, NOT resolved/common-rho calibration. No rho grid
  or rho selection in this run; no temporal-block kernel or signal/prior change.
- Four distinct truth-blind starts occupy 1/3/7/10 classes; 600 sweeps per chain,
  first300 warmup, thin1, checkpoints every50. Single worker, numerical threads1.
- Explicitly enable D-043 FFBS caching and passive observations. Guidance scale
  starts at .25; the existing controller uses stop200, batch25, target .44,
  gain1, offset1, exponent .6, lower .05, upper4. Eight warmup updates affect
  the NEXT batch only. Sweeps201–300 are fixed-scale settling;301–600 retained.
  The policy is a provisional warmup choice, not an empirically calibrated result.
- New seeds: innovations2026091401, profile permutation2026091402, unit
  permutation2026091403, responses2026091404; starts2026091411–1414;
  chains2026091421–1424. No additional PG moment study; the unchanged exact-PG
  backend is used in actual transitions, with prior moment evidence retained.
- Hard worker budget600 seconds, dispatcher630, whole batch2700 (45 minutes).
  Reserve a complete dispatcher plus30 seconds before the next start. Failure,
  timeout or insufficient remaining budget stops the batch without retry/resume.
  No thresholds change after seeing results. All partial outputs are retained.

## Identity and execution

`R/graphmode_dev_run.R` / `scripts/graphmode-dev-run.R` are new additive files;
all 31 historical runtime files and four audited D-043 files stay unchanged.
New identity includes those manifests/files plus the new module, CLI, fixed-input
tests and this guide. Git commit/file closure, loaded function bodies/constants,
recursive R/backend identities, C locale and thread controls must match.

Preparation creates only a new canonical external registration; data generation
is inside separately authorized `run`. Persist generated input and all four
signed envelopes before any chain. The worker receives only fit/initial state,
not generating truth. New envelopes bind the original controlled plan plus the
complete new source, cache/observation policy and experiment registration.

The execution adapter privately rebinds the audited controlled driver: the sweep
calls `graphmode_dev_sweep`; guarded writes add the D-044 schema and actual
development observations to each checkpoint. Checkpoint/source/failure/timing
mechanics otherwise reuse the old driver; old functions are not globally replaced.
The D-043 source is not duplicated and no installed package interface is changed.
New results cannot enter the old controlled/rho acceptance contract as old runs.

Verification before freezing: 19 new fixed-input/mocked-process groups pass,
including actual reused worker/checkpoint execution with deterministic tapes,
new-source loading, schema/observation binding, tuning replay, timeout rejection,
successful postflight receipt, failed postflight, alias normalization and first-
failure batch stop. No scientific samples are generated by these tests. The
independent D-043 audit's 26+9 checks and the existing 31+4 source hashes are
reused and verified, not counted as new execution tests in this turn.

Final acceptance requires successful worker exit, fresh source/runtime/path
postflight, unchanged envelopes, complete original numerical evidence, complete
actual development observations and a result/execution-bound receipt. Validation
replays guidance control from stored decisions, verifies freeze and checks the
observed actual MH corrections/movement and node-move events against the core.
An in-memory compatibility view permits reuse of the numerical auditor; it is
never written as an old-format result or resumable checkpoint.

Use only the NEW entry: `status`, `check`, `prepare NEW_EXTERNAL_DIR`,
`preflight REGISTRATION.rds`, `run REGISTRATION.rds --authorized`,
`diagnose REGISTRATION.rds`. Default is status, not execution. The top launcher
keeps terminal.log and raw batch exit; chain execution records retain console
output even if no acceptance is issued. Writes are immutable, exclusive, and
never rebuild disappeared output trees.

## Diagnosis, not a release rule

After successful completion, inspect four-chain rank/folded Rhat, bulk/tail ESS
and PSM using unchanged r4 thresholds (1.01 / 400 / .05), including explicit
uninformative constant-discrete handling. Also report complete/occupied/empty
expert time, extra observation time, numerical guards, rejection spells, actual
proposal and guidance movement, and allocation probabilities throughout retention.
Four starts are independent and use the same target, with proposal scales frozen
within retention. A passing necessary check still does not prove all-mode mixing.

There is no matched uncached comparison arm and data/warmup differ from D-041.
Do not infer a causal mixing improvement or cache speedup factor by comparing the
two experiments. Record actual cost of this run; additional benchmark/calibration
requires another plan and authorization. No paper result, rho selection, package
integration, upstream overwrite, automatic extension or formal simulation follows.
