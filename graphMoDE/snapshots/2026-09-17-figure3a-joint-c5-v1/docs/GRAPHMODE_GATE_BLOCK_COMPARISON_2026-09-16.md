# D-061：门控整体／条件分块配对开发对照（准备稿）

2026-09-16。用户告知已完成一轮审计并要求继续。本轮准备独立执行接口、
固定输入检查和前瞻预算；未冻结提交、外部登记或运行模拟。

## 审计交接与范围

已读取本机 D-060 独立审计报告，核对其 HEAD d357f04、五项受审文件指纹
及原80项冻结清单，全部一致。审计记录24组原检查、四次独立完整外层
对照、两项真实非恒定似然的固定输入预算边界检查通过，未发现新增可复现
正确性缺陷。本轮复用这些证据，不重新审计核心或计算旧 c2 的诊断统计。
报告身份见本轮 provenance。D-060 文档的“尚未审计”是历史状态，现已被
这次独立审计取代；其原文件保持不变以保留受审指纹。

本次新执行层不在该审计范围内。D-060 是保持同一条件目标的计算步骤
变体，尚未证明改善混合，也不是论文已明确采用的算法。block42 原五项
失败、总体 valid=FALSE 和旧 B/c1 结论仍保留。

## 前瞻设计（待批准）

仅一份新的 spiral 合成面板：n=121、T=168、真实5类、拟合K=10，
graphMoDE-W。新数据和完整起点均不读取旧 c2/B/c1 的状态或检查点。
四份分散起点覆盖 Z、x、v、theta，初始占用为1/3/7/10；两组共用同一
数据和同编号起点，采样流独立。A 仅提供已审查固定 guidance 尺度
2.2688962687759413；rho=4仍是临时工程设置，不宣称已校准。

| 项目 | full基准 | contrast候选 |
|---|---|---|
| 门控ESS | 原2187维整体ESS | 固定顺序9组，每组243个白化坐标 |
| 专家路径 | block42，offset每外层均匀取0–41 | 相同 |
| 门控内循环m | 4 | 4 |
| 每扫描候选评估上限 | 1000 | 全部9组共享1000 |
| 每外层guidance提议 | 40 | 40 |
| 每外层最终分配 | 1 | 1 |
| 四链长度 | 每链1200外层步，丢弃600，保留600 | 相同 |
| 自适应/thinning | 无自适应，thin=1 | 相同 |

full在这里指门控整体ESS，两组专家都使用block42。单线程串行，顺序为
full1、contrast1、contrast2、full2、full3、contrast3、contrast4、full4。
条件块及内扫描不是后验样本。任何预算耗尽均失败停止，不把部分扫描
作为拒绝样本，不自动重试、补链、增步或切换策略。

候选种子：数据2026091901–1904；Z为2026091911–1914，v为
2026091921–1924，x为2026091931–1934，theta为2026091941–1944；
full链2026091951–1954，contrast链2026091961–1964。这里只检查草案
内部及相邻c1/c2种子的区别；全历史登记碰撞检查留给批准后的登记步骤。
建议新外部目录为
`/Users/liuzw/countDLM-local-results/graphmode-gate-blocks-20260916-c3`，
尚未创建，登记时必须再次确认不存在，不能复用旧目录。

## 预算与成本口径（待批准）

- 每worker硬上限1800秒，整步边界预算1740秒，下一个worker启动预留
  1920秒；预热、保留、校验和I/O均计入。
- 科学批次16200秒（4.5小时），独立诊断1800秒（0.5小时），合计
  **5小时过程上限**，父层只读哈希和验收另有开销。两组使用同一上限。
- 沿用16GiB输出配额、起始空闲至少32GiB、运行中至少5GiB；每300步
  保存新检查点。所有失败、超时和已完成状态均保留，不支持恢复续跑。

5小时是未测量新策略成本前的保守草案，不是预计耗时或新的运行授权。
本设计匹配数据、起点、外层样本数和所有其他设置；两组实际耗时允许不同，
并非保证相等的墙钟时间。比较效率必须使用四链完整worker秒数作为
ESS/秒分母，该分母含预热、保留与I/O；同时单列门控总成本，不能只比较
局部门控接受率。

## 执行与验收

新入口 `scripts/graphmode-gate-blocks-run.R` 默认仅status；check仅执行
固定输入/进程返回值替身检查。prepare要求批准方案且源码已提交冻结；
run和diagnose分别要求明确授权。科学结束不会自动诊断，任何阶段都不
自动继续。旧命令、登记、检查点和单步门控字段不能用于本方案。

复用旧执行器的来源、加载定义、环境、路径、一次性输出、进程退出和
pending→final验收保护。新schema保存D-060的ess_policy和每次内扫描的
逐块评估数及白化位移，验收索引策略、完整扫描、共享预算以及每扫描位移
加和。整次扫描ESS计时包含全部块及其记录；原guidance计时单列。
保留完整外层theta以核查专家块边界与拒绝状态；旧专家/外层校验只读取
已检查过新字段的内存视图，持久化结果始终保持新schema和原对象摘要。

原四链validity与报告检查器继续使用全部37个标量、六对组内PSM：
rank/folded Rhat≤1.01、bulk/tail ESS≥400、PSM RMS≤0.05。两组分别
计算，常数、未定义值和失败保留原含义。不因已观察的五项失败删减指标。
新结果即使全部通过，也不自动证明全局混合、采用默认策略或放行正式模拟。

## 验证与下一步

本轮新增检查及确切来源指纹见
`provenance/graphmode-gate-block-comparison-2026-09-16.md`。
下一步先审查这份执行接入和设计预算，再批准范围限定的冻结与登记。
真实科学运行和一次性诊断仍需要各自授权。未修改论文、包接口、旧冻结
代码、验收门槛或旧结果，也未同步至外部共享位置。
