# Package metadata

Package metadata is maintained directly in `bdynets/DESCRIPTION`. The package
help topic lives in `bdynets/R/bdynets-package.R`. This document no longer runs
`usethis::create_package()` or rewrites dependencies.

The original setup is preserved in `docs/legacy-litr/1description.Rmd.txt`.

```{r inspect-metadata, eval=FALSE}
read.dcf("bdynets/DESCRIPTION")
```
