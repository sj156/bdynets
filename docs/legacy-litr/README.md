# Historical litr setup

These `.Rmd.txt` files preserve the original metadata generation, render and
documentation instructions from before the standard-package migration.
They are inert reference text, not active build inputs. In particular, the old
index contains regeneration/deletion examples that must not be run against the
maintained package.

The other original root R Markdown chapters remain in place as historical
mathematical notes and analysis examples. Code is now maintained in `bdynets/R/`.
Use the repository README for the current document/test/build workflow.
